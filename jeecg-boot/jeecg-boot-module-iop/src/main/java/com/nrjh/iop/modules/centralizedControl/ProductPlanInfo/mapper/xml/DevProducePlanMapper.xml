<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nrjh.iop.modules.centralizedControl.ProductPlanInfo.mapper.DevProducePlanMapper">
<select id="selectNumMap" resultType="java.util.Map">
SELECT sum(EQUIP_NUMBER) num,EQUIP_CATEG categ
FROM DEV_PRODUCE_PLAN
WHERE PRODUCE_CYCLE_ID = #{id}
GROUP BY EQUIP_CATEG
</select>
</mapper>